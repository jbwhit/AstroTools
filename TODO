- Create auto-bashrc link to add the github folder/AstroTools/stuff to path.

Create git directory for fitting each absorber.

Add
- shreklogin
- scpdesktop/green/etc.
- helpreduce.py
- various update functions (moebius/shrek/green/etc.)

 =======
 = FIT =
 =======
- check for matching UPPER and lower case components
-- isupper(); islower()
-- len(string_of_) = set(string_of_)
- Rewrite in python (to make more flexible)
- create function that reruns using everything exactly in the fitting directory.
